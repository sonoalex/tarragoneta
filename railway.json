{
  "$schema": "https://schema.up.railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "preDeployCommand": "export FLASK_APP=app.py && python compile_translations.py && (flask db upgrade || (flask db stamp 6d646413299d && flask db upgrade))"
  }
}
